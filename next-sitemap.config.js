/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: 'https://ai-faq-demo.vercel.app',
    generateRobotsTxt: true,
    sitemapSize: 7000,
    exclude: [], // 如不想让某些页面出现在 sitemap，可以在这里列出
  };
  